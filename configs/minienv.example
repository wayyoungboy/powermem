# =============================================================================
# Minimal Configuration for PowerMem
# =============================================================================
# This is the smallest configuration needed to get started with PowerMem.
# Copy this file to .env and fill in your API keys.
# Only required and non-default values are included here.

# Database Configuration
DATABASE_PROVIDER=sqlite
DATABASE_PATH=./data/powermem_dev.db

# LLM Configuration (required)
# Choose LLM provider: qwen, openai ...
LLM_PROVIDER=your_llm_provider
LLM_MODEL=model_name
LLM_API_KEY=your_api_key_here

# Embedding Configuration (required)
EMBEDDING_PROVIDER=your_embedding_provider
EMBEDDING_MODEL=your_embedding_model_name
EMBEDDING_API_KEY=your_api_key_here

